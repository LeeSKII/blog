---
import type {IAnimateProps as Props} from '@/types'
const {animate='fade'} = Astro.props
---
<code transition:animate={animate} class="relative rounded bg-muted px-[0.3rem] py-[0.2rem] font-mono text-sm font-semibold">
  <slot />
</code>