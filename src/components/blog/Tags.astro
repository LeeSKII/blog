---
import Tag from "./Tag.astro";
interface Props {
  tags: string[];
}
const { tags } = Astro.props;
---

<div class="tags">
  {tags.map((tag) => <Tag url={`/blog/tags/${tag}`} name={tag} />)}
</div>

<style>
  .tags {
    margin-top: 20px;
    display: flex;
    flex-wrap: wrap;
  }
</style>
